<div class="container">
  <div class="row">
    <div class="col-md-8">
      <section class="" >
      <div class="container">
         <div class="card mask-custom">
            <div class="card-body p-4 text-white">
            <div class="well-name" >
               <h3 class="welcome">
                  Hi <%= @current_user.username %>! Today is <%= @today %>
               </h3>
            </div>
            <%= render "form" %>
            </div>
         </div>
         <%= render "todos/progress" %>
         <div class="card mask-custom">
            <div class="card-body p-4 text-white">
            <div class="text-center pt-3 pb-2">
               <img src="https://mdbcdn.b-cdn.net/img/Photos/new-templates/bootstrap-todo-list/check1.webp"
                  alt="Check" width="60">
               <h2 class="my-4">Task List</h2>
            </div>
            <%= render "todos/table" %>
            </div>
         </div>
      </section>
    </div>
      <div class="col-md-4"><!-- circle cards with resources --->
         <%= render "todos/circle_card" %>
         <%= render "todos/chat_card" %><!-- trasnparent card for Chat GPT --->  
    </div>

   </div>
</div>
