<!-- this form adds the task to the table but does not include the subject (category)-->

<form action="/insert_todo" method="post">
  <div class="form" style="display:flex;">
    <label for="category_id_box">Subject:</label> 
    <select id="category_id_box" name="category_id">
      <% Category.all.each do |option| %>
        <option value="<%= option.id %>"><%= option.description %></option>
      <% end %>
    </select>
      <label for="content_box">
         Describe the task:
      </label>

     <input type="text" id="content_box" name="query_content">
    <button class="add">
      Add to list
    </button>
  </div>
</form>

  
<!-- this form LOOKS good but is pretty much useless -Paris Hilton- 

<div class="form">
  <form action="/insert_todo" method="post">
    <div class="input-group mb-3">
      <label class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false" for="category_id_box">Subject</label>
      <select class="dropdown-menu" id="category_id_box" name="category_id">
          <% Category.all.each do |option| %>
            <option value="<%= option.id %>"><%= option.description %></option>
          <% end %>
      </select>
      <input for="content_box" type="text" id="content_box" name="query_content" class="form-control" aria-label="Text input with dropdown button" placeholder="New task">

      <button type="button" class="btn btn-secondary" style="$teal-600">Add</button>
    </div>
  </form>
</div>-->
